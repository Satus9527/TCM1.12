# 立即执行 - 清理并重新迁移

## 🔧 问题：索引名称重复

**错误**: `Duplicate key name 'user_files_user_id'`

**原因**: 外键自动创建的索引与手动定义的索引名称冲突

---

## ⚡ 立即执行（3步）

### 1️⃣ 手动删除已创建的表
```powershell
# 在 PowerShell 中执行（在 D:\TCM web 目录）
mysql -u root -p
```

然后在 MySQL 中执行：
```sql
USE tcm_platform;

-- 删除表（按顺序）
DROP TABLE IF EXISTS user_simulations;
DROP TABLE IF EXISTS user_files;

-- 确认删除
SHOW TABLES;

-- 退出
EXIT;
```

---

### 2️⃣ 等待文件修复

我正在修复迁移文件中的索引名称冲突...

---

### 3️⃣ 重新运行迁移
```powershell
cd "D:\TCM web"
npx sequelize-cli db:migrate
```

---

**立即执行第1步，我会马上修复文件！**

